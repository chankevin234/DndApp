#include "condition.hh"
#include <iostream>
#include <string>

// Constructor
// :: indicates that this is refering to the Class "condition" constructor/functions in header
Condition::Condition(string toBeNamed) {
    name = toBeNamed;
}
